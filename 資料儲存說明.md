# 資料儲存說明

## 🎉 v1.2.0 完全同步功能

**所有資料現在都儲存在檔案中！** 複製整個資料夾到任何電腦，所有資料都會完全同步！

## 📁 資料檔案位置

所有資料統一儲存在應用程式目錄下的 `data/` 資料夾：

```
分攤計算器-win32-x64/
├── 分攤計算器.exe
├── data/                    ← 所有資料都在這裡！
│   ├── history.json        ← 歷史結算記錄
│   ├── current.json        ← 當前帳目與成員列表
│   └── settings.json       ← 設定選項
├── resources/
└── ...
```

### 檔案說明

#### 1. history.json - 歷史結算記錄
儲存所有已結算的記錄，包括：
- 結算日期與金額
- 每個人的應付/實付金額
- 債務清算方案
- 大哥額度使用情況

#### 2. current.json - 當前資料
儲存當前的即時資料：
- **成員列表** (例如：二哥、大哥、三弟、倩瑜)
- **當前帳目** (尚未結算的費用項目)

#### 3. settings.json - 設定選項
儲存你的偏好設定：
- 上期餘額開關 (applyPreviousBalance)
- 公平分攤模式 (useFairSplit)
- 大哥額度 (daGeQuota)

## 💡 完全同步的優點

### ✅ 所有資料都會同步
- ✅ 歷史結算記錄
- ✅ 當前未結算的帳目
- ✅ 成員列表
- ✅ 所有設定選項（公平分攤、大哥額度等）

**不再有任何資料綁定在電腦上！**

### 🌟 使用情境

#### 情境 1：辦公室 + 家裡
- 在辦公室輸入了一些帳目但還沒結算
- 把整個資料夾複製到 USB 或雲端
- 回家開啟，**所有未結算的帳目都還在**
- 繼續輸入或進行結算

#### 情境 2雲端硬碟自動同步（最推薦）⭐⭐⭐
將整個 `分攤計算器-win32-x64` 資料夾放在雲端硬碟：
- **OneDrive** (Windows 內建)
- **Google Drive**
- **Dropbox**

**效果：** 
- 在辦公室電腦輸入費用 → 自動同步到雲端
- 回到家打開筆電 → 立即看到最新資料
- 任何修改都會即時同步所有裝置

### 方式 2：USB 隨身碟攜帶
把整個資料夾放在 USB 隨身碟：
- 插到任何電腦都能用
- 資料永遠跟著你走
- 不需要網路

### 方式 3：手動備份
定期複製 `data/` 資料夾到安全位置：
```
備份/
  ├── 2026-05-18-備份/
  │   ├── history.json
  │   ├── current.json
  │   └── settings.json
  └── 2026-05-01-備份/
      └── ...
```
3. 需要還原時貼回 `data/` 資料夾即可

### 從舊版本升級

**v1.2.0 會自動遷移！** 首次啟動時：
1. 自動從 LocalStorage 讀取舊資料
2. 轉存到檔案中 (current.json, settings.json)
3. 清除 LocalStorage 中的舊資料
4. 完成遷移！

你不需要做任何事，程式會自動處理。

## ⚠️ 重要注意事項

### ✅ 要複製整個資料夾
```
📁 分攤計算器-win32-x64/          ← 整個資料夾！
   ├── 分攤計算器.exe
   ├── data/                      ← 三個檔案都要在
   │   ├── history.json
   │   ├── current.json
   │   └── settings.json
   ├── resources/
   └── ...
```

### ❌ 不要只複製 exe
只複製 `分攤計算器.exe` → **所有資料都會遺失！**

## 優點總結

✅ 所有資料儲存在檔案，不再依賴瀏覽器  
✅ 複製整個資料夾 = 完整遷移所有資料  
✅ 支援雲端同步，多台電腦自動更新  
✅ 可直接用文字編輯器查看/編輯資料 (JSON 格式)  
✅ 備份、還原超級簡單  
✅ 不需要網路或資料庫，完全離線運作

✅ 資料與應用程式在同一位置，方便管理  
✅ 可直接複製整個資料夾到其他電腦使用  
✅ 備份、還原簡單直觀  
✅ 適合使用雲端同步工具  
✅ 可以用文字編輯器直接查看/編輯資料

## 注意事項

- 當前帳目和成員設定仍儲存在 LocalStorage（臨時資料）
- 只有已結算的歷史紀錄會儲存在檔案中
- 檔案採用 JSON 格式，可直接用文字編輯器查看
- 刪除 `data/history.json` 會清空所有歷史紀錄（請謹慎操作）
